<%= form_for(@site) do |f| %>
  <% if @site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% @site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 2 %>
  </div>
  <div class="field">
    <%= f.label :type_id %><br />
    <%= f.collection_select(:type_id, Type.find(:all, :order => :name), :id, :name) %>
  </div>
  Location
  <div id="sitesmap_canvas" style="width:350px; height:200px"></div>

  <div class="field">
    <%= f.label :latitude, :hidden => true %><br />
    <%= f.text_field :latitude, :hidden => true %>
  </div>
  <div class="field">
    <%= f.label :longitude, :hidden => true %>
    <%= f.text_field :longitude, :hidden => true %>
  </div>
  <div class="field">
    <%= f.label :zoom, :hidden => true %>
    <%= f.text_field :zoom, :hidden => true %>
  </div>

  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.text_field :image_url %>
  </div>
  
  <div class="field">
    <%= f.label :visits, :hidden => true %>
    <%= f.text_field :visits, :hidden => true %>
  </div>
  
<script type="text/javascript">
    sitesmap();
</script>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>

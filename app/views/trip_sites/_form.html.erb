<%= form_for(@trip_site) do |f| %>
  <% if @trip_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip_site.errors.count, "error") %> prohibited this trip_site from being saved:</h2>

      <ul>
      <% @trip_site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trip_id %><br />
    <%= f.number_field :trip_id %>
  </div>
  <div class="field">
    <%= f.label :site_id %><br />
    <%= f.number_field :site_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
